{
  "name": "@docmate/core",
  "version": "0.1.0",
  "description": "Core business logic for DocMate",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b --watch",
    "clean": "rimraf out *.tsbuildinfo",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@docmate/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "files": [
    "out/**/*"
  ]
}
