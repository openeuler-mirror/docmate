# VS Code Extension Ignore File
# 排除所有源代码和开发文件，只保留打包后的产物

# 源代码和开发文件
packages/**
src/**
**/*.ts
**/*.tsx
**/*.map
*.config.js
*.config.ts
tsconfig*.json
esbuild.config.js

# 开发工具和配置
.vscode/**
.vscode-test/**
.gitignore
.gitattributes
.augment/
.eslintrc.*
.prettierrc.*
jest.config.*
vitest.config.*
vite.config.*

# 依赖和缓存
node_modules/**
*.tsbuildinfo
.nyc_output
coverage/
.coverage/
.cache
.parcel-cache

# 测试文件
**/test/**
**/tests/**
**/*.test.*
**/*.spec.*

# 文档（保留必要的）
docs/**
*.md
!README.md
!README.en.md
!CHANGELOG.md

# 包管理器文件
pnpm-lock.yaml
yarn.lock
package-lock.json
.pnpmfile.cjs
.yarnrc.yml

# IDE和编辑器文件
.idea/**
*.swp
*.swo
*~

# 系统文件
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 日志文件
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# 临时文件
tmp/
temp/
pids
*.pid
*.seed
*.pid.lock

# 缓存目录
.npm
.eslintcache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/
.node_repl_history
*.tgz
.yarn-integrity

# 环境变量文件
.env*

# 构建输出（除了最终产物）
.next
.nuxt
.out
.storybook-out

# 后端文件
packages/backend/**
*.py
*.pyc
__pycache__/
*.pyo
*.pyd
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# 项目管理文件
issues/**
scripts/**

# 只保留以下文件：
# - dist/extension.js (esbuild打包后的扩展代码)
# - dist/ui/** (UI组件的构建产物，由esbuild复制)
# - package.json (扩展元数据)
# - README.md, README.en.md, CHANGELOG.md (文档)
# - LICENSE (许可证)
# - images/** (图标等静态资源)

# 确保包含必要的产物目录
!dist/
!images/
